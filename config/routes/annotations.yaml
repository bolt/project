
# All routes, used by Bolt: Control Panel, Async and Thumbs, unless overridden
# in `../routing.yml` or another location
controllers:
    resource: '../vendor/bolt/bolt/src/Controller/'
    type: annotation

